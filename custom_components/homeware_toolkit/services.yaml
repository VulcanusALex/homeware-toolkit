run_command:
  name: Run homeware command
  description: Run a homeware CLI subcommand on the configured gateway.
  fields:
    command:
      name: Command
      description: Subcommand and arguments, e.g. "doctor --key ~/.homeware-toolkit/id_rsa".
      required: true
      selector:
        text:
